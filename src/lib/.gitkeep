# Local game DLLs (sts2.dll, GodotSharp.dll, 0Harmony.dll, plus optional transitive deps)
# are copied here by the developer once after install — never committed.
# See docs/notes/mod-loader-api.md for the list of required references.
